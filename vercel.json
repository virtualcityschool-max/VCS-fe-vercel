{
  "buildCommand": "node scripts/generate-sitemap.js && npm run build && node scripts/prerender.js",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/shell.html" }
  ]
}
